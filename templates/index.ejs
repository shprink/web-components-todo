<!DOCTYPE html>
<html dir="ltr" lang="en">

<head>
    <meta charset="utf-8">
    <title>Web Components Todo</title>
    <meta name="Description" content="A simple todo list built with various Web Components technologies">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, minimum-scale=1.0, maximum-scale=5.0">
    <meta name="theme-color" content="#16161d">
    <meta name="apple-mobile-web-app-capable" content="yes">

    <meta http-equiv="x-ua-compatible" content="IE=Edge" />

    <link rel="apple-touch-icon" href="/assets/icon/icon.png">
    <link rel="icon" type="image/x-icon" href="/assets/icon/favicon.ico">
    <link rel="manifest" href="/manifest.json">
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO"
        crossorigin="anonymous">
</head>

<body>
    <div class="container">
        <div class="py-5 text-center">
            <h1>Web Components Todo</h1>
            <p class="lead">The same todo application built in different Web Components libraries for benchmark purpose</p>

            <p>Repo:
                <a href="https://github.com/shprink/web-components-todo" target="_blank">github.com/shprink/web-components-todo</a>
            </p>
            <p>The demos do not use any Polyfill for benchmarks purpose. <br /> Please use Chrome, Safari or Firefox >
                v63</p>
        </div>
        <table class="table table-striped table-dark">
            <thead>
                <tr>
                    <th scope="col">Web Components</th>
                    <th scope="col" style="width: 70px"></th>
                    <th scope="col" style="width: 70px"></th>
                    <th scope="col">Size Gzipped (in Kb)</th>
                </tr>
            </thead>
            <tbody>
                <% WCprojects.forEach(function(project){ %>
                    <% if (project.group) { %>
                        <%- include('group', {row: project}); %>
                    <% } else { %>
                        <%- include('row', {project: project}); %>
                    <% } %>
                <% }); %>
            </tbody>
        </table>
        <table class="table table-striped table-dark">
            <thead>
                <tr>
                    <th scope="col">Classic Frameworks (not Web Components)</th>
                    <th scope="col" style="width: 70px"></th>
                    <th scope="col" style="width: 70px"></th>
                    <th scope="col">Size Gzipped (in Kb)</th>
                </tr>
            </thead>
            <tbody>
                <% NonWCprojects.forEach(function(project){ %>
                <tr <% if (project.slug === 'native') { %>class="table-light" <% } %>>
                    <td class="name"><%= project.name %></td>
                    <td><a href="<%= project.demo %>">Demo</a></td>
                    <td>
                        <% if (project.docs) { %>
                            <a href="<%= project.docs %>" target="_blank">Docs</a>
                        <% } %>
                    </td>
                    <td>
                        <div class="progress">
                            <div class="progress-bar" role="progressbar" style="width: <%= Math.floor((project.size / maxSize) * 100)%>%" aria-valuenow="<%= Math.floor((project.size / maxSize) * 100)%>"
                                aria-valuemin="0" aria-valuemax="100"><%= project.size.toFixed(1) %></div>
                        </div>
                    </td>
                </tr>
                <% }); %>
            </tbody>
        </table>
    </div>

    <style>
        body {
            background: #f5f5f5;
        }
        .progress {
            background-color: #525252;
        }
        .table-light .name {
            color: #333;
            font-size: 1.2rem;
        }
        table {
            width: 100%;
            table-layout: fixed;
        }
    </style>
</body>

</html>